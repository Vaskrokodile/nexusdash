{
  "name": "nexus-dash",
  "version": "1.0.0",
  "description": "Premium client dashboard platform with liquid glass morphism",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "seed": "node server/seed.js",
    "install:all": "npm install && cd client && npm install",
    "build": "cd client && npm run build && cd .. && cp -r client/dist dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.3.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}